//
//  Use this file to import your target's public headers that you would like to expose to Swift.
//
//  The legacy `#import <igi_sdk/igi_sdk.h>` was needed when this app
//  consumed the Obj-C `igi_sdk` framework via SPM and used a Swift
//  bridging header to expose Obj-C symbols to Swift code. The new
//  Swift SDK is a Swift module — `import igi_sdk` from any
//  .swift file is enough; no bridging header indirection required.
//  The file is kept (rather than deleted) so the project's
//  SWIFT_OBJC_BRIDGING_HEADER build setting still resolves; if other
//  Obj-C deps land in the future, add their `#import`s here.
